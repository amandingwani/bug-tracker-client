export interface AppInnerProps {
  list: Array<any>;
  subheader?: string;
  title: string;
}

export interface AppChartProps {
  chart: any;
  subheader?: string;
  title: string;
}

export interface Tasks {
  id: string;
  name: string;
}
